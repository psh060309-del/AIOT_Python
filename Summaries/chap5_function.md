---

### 📄 chap5.md (함수)

```markdown
# 📂 [chap 5] 함수 (Functions)

### 1. 함수의 정의와 호출 📞
- **정의**: `def 함수이름(매개변수...):`
- **호출**: `함수이름(인수)`
- **pass**: 내용물을 나중에 작성하고 싶을 때 사용하는 키워드입니다.

### 2. 다양한 인수(Argument) 전달 방식
- **디폴트 인수**: 매개변수에 기본값을 지정합니다. (`msg="별일없죠"`)
- **키워드 인수**: 인수의 이름을 명시하여 순서와 상관없이 값을 전달합니다. (`x=10, y=20`)
- **가변 인수 (*args)**: 매개변수 개수가 정해지지 않았을 때 튜플 형태로 전달받습니다. 📦
- **가변 키워드 인수 (**kwargs)**: 인수를 딕셔너리 형태로 전달받습니다. 📖

### 3. 언패킹 (Unpacking) ✨
- `*`: 리스트나 튜플 같은 반복 가능한 객체를 개별 요소로 풉니다.
- `**`: 딕셔너리 객체를 키-값 쌍으로 언패킹합니다.

**언패킹 예시:**
```python
numbers = [1, 2, 3]
show_numbers(*numbers) # show_numbers(1, 2, 3)과 동일하게 작동
